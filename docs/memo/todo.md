# やることリスト


# 残タスク一覧（完成までのTODO）

対象：7月末〜8月中旬の完成に向けた残作業。
方針：**まず縦に一本（開始→戦闘→ボス→クリア）を通す**ことを最優先。仕上げは後。

---

## 1. カメラ・操作（完全3D化）
- [x] マウスでのカメラ方向変更（回転）
- [x] WASD移動をカメラ相対に変更
- [x] マウスカーソルの制御（ゲーム中は非表示／画面内固定）
- [x] ダッシュ（Shift・dashMultiplierをJSONから参照）
- [x] 画面中央レティクル（照準ガイド）のUI描画 ※コード描画・敵捕捉で赤色に切替（TargetingSystem + AimComponent）
- [x] 遠距離はカメラ前方（レティクル方向）基準で発射
- [ ] 近接攻撃もカメラ前方基準に（現状は全方位の距離判定のまま）

## 2. プレイヤー攻撃
- [ ] Attack1 → Attack2 のコンボ接続（近接）
- [ ] 攻撃アニメの当たり判定ウィンドウ連動（発生・持続フレーム）
- [x] 遠距離：Window投擲（右クリック・手動照準・カメラ前方へ発射）※見た目は仮スフィア
- [ ] 被弾時の無敵時間（1.0秒＋白フラッシュ）
- [ ] ヒットストップ等の手応え（余裕があれば）

## 3. 敵AI（AISystemの行動分岐）
- [ ] 攻撃要求を「攻撃レンジ内のときだけ」に修正（現状は距離無視でCDごとに要求）
- [ ] 攻撃時に Attack1 アニメを要求＋時間ウィンドウ判定
- [ ] Xcode：近接追従（現状ベースを整理）
- [ ] Safari：キープディスタンス（近すぎたら後退・ホバー高度維持）
- [ ] Mac（ボス）：距離帯で行動分岐（近接／魔法／走り）

## 4. 投射・弾（ProjectileSystem 新規）
- [x] ProjectileSystem（生成・移動・衝突・寿命）※既存Component流用構成、パラメータはprojectileData.jsonから設定
- [x] プレイヤー：Window投擲（手動照準・カメラ前方へ発射）
- [ ] 弾の見た目をWindowビルボードに差し替え（現状は仮スフィア）。余裕があれば実WindowAPI連携
- [ ] Safari：Webサイト弾（ビルボード）
- [ ] ボス：虹色カーソル魔法弾
- [x] 弾のヒット判定 → ダメージ処理（AttackSystem流用・陣営タグで自弾の誤爆防止）
- [ ] 長押しで溜めることができ、一定時間以上になると威力大きさ、などが上がるように

## 5. ボス戦フロー
- [ ] 雑魚全滅イベント → ボス出現
- [ ] ボス出現時に Boss BGM へ切り替え
- [ ] ボス撃破 → Result（勝利）へ遷移

## 6. 死亡・ダメージフロー
- [ ] Dyingアニメ → AnimationFinished → 非表示（現状は即非表示）
- [ ] CoRチェーンを DamageCalculator へ置換（合意済み）

## 7. ステージ制作（B案：手作り1ステージ）
- [ ] Web製レベルエディタ（別リポジトリ）：敵スポーン／障害物・配置物／プレイヤー開始位置／ボス出現位置 を配置 → JSON書き出し（BGMは対象外）
- [ ] JSONスキーマ拡張：配置物（モデル・位置・回転・スケール）＋コライダー種別（AABB or メッシュ）
- [ ] メッシュ判定対応：曲面・山型地形は MV1SetupCollInfo / MV1CollCheck_Line で接地・壁ずり
- [ ] 配置物モデルの読み込み・描画（ModelRepository / 描画側の拡張）
- [ ] AI生成でステージ用モデル・テクスチャ作成
- [ ] 進行：雑魚全滅でボス出現 → 撃破でクリア（1ステージ完結）

## 8. エフェクト（Effekseer）
- [ ] 攻撃ヒット・被弾・ボス魔法・死亡 などのエフェクト作成／登録

## 9. バランス調整・仕上げ
- [ ] HP／攻撃力／レンジ／クールダウンの調整
- [ ] playerData.json のデバッグ値を本番値へ（defence 100000 / attackPower 0）
- [ ] デバッグ描画の削除（drawDebugVisuals・青/赤/黄、drawDebugSphere/Capsule 一式）
- [ ] 通しプレイでのバグ潰し



## Loadin画面の仕様について
- ctrl + Cでアプリケーションを終了するようにする

## InGameを実装するときにぶつかる大きな壁
1. モデルのアニメーション
2. マップの作製（オブジェクトの配置）

### モデルのアニメーション方針
- **Player / Enemy（アニメーションあり）**: pmx + vmd を使用
  - Mixamo FBX → Blender（mmd_tools アドオン）→ MMD標準ボーン名に対応付け → pmx / vmd エクスポート
  - DxLibはpmxもMV1系APIで透過的に扱えるため、既存の `Animator` / `ModelRepository` のコード変更は不要
- **地形・建物・Stage（静的オブジェクト）**: mv1 のまま継続

## ポインタ不要箇所 再評価

### 実装予定
- [ ] `Title::m_fade` を `std::optional` に変更
- [ ] `Select::m_fade` を `std::optional` に変更
- [ ] `PlayerFactory::getPlayer()` に assert を追加（early detection）

## 開発が進んだときに行うべきこと
- アセット内のファイルをすべて小文字 + スネークケースに直す

- resource.jsonにリソースではなく、実データが混入しているため分離を検討
  "effects": [
    {
      "type": "Hit",
      "path": "assets/effects/slash.efk",
      "poolSize": 10,
      "yOffset": 75.0,
      "scale": 10.0
    }


### リファクタリング
- Syetem関連のクラスでUpdate内で毎回GetComponentしているため、ISystemクラスに最初にコンポーネントを取得するような純粋仮想関数を追加する -> それを具象クラスですべてのSystemでキャッシュする

- 初期化の際は｛｝で初期化リストで初期化を行うべきだが、代入も{}にしてしまうと可読性が下がることが分かったため、
初期化は｛｝で、代入は = で書く様にする

- 現在DxLib.hを直接インクルードしているが、ラッパーのヘッダーファイルがあったほうが安全かつ差し替えるときに容易になるため作るようにする

- InGame.cpp内のhファイルが多いためhファイルをインクルードする専用クラスを作成するか検討中

- InGame,InGameViewクラスの責務がたくさんあり、これらを分離するかを検討する

- 一度選択した職業やファイルは記憶しておいて再度挑戦するときにそのまま選択状態にされておくようにする